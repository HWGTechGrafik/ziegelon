node_modules/
dist/
dist-ssr/
*.local
.DS_Store
.vite/
coverage/
*.tsbuildinfo

# Ergebnisse der Windows-Fassung: ueber 100 MB, gehoeren nicht in die
# Versionsverwaltung. Gebaut wird mit 'npm run build' und
# 'dotnet publish -c Release windows'.
windows/bin/
windows/obj/
release/
*.log
.env
.env.*
!.env.example

# Lizenzgenerator: enthaelt den privaten Schluessel und darf nie ins Repo.
# Bleibt hier stehen, damit die Sperre auch in einem frischen Klon greift.
privat/

# Fertig geschnuerte Fassung zum Weitergeben: dieselbe exe wie unter release/
# samt Anleitung und gepackt. Ueber 100 MB, gehoert nicht in die
# Versionsverwaltung. Wird mit 'npm run auslieferung' neu gebaut.
auslieferung/
